Snake game which runs on raspberry pi5.
The pins used are different to use software SPI to connect to the 8x8 LED matrix.
This is done because this setup was running on raspberry pi5 on a pironman setup where pironman uses the hardware SPI pins, so had to avoid the conflicts.
